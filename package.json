{
  "name": "dtc-webhook",
  "version": "1.0.0",
  "description": "DTC WhatsApp Cloud API Webhook for Netlify",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Functions-only deployment, no build needed'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/functions": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "netlify-cli": "^21.6.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
